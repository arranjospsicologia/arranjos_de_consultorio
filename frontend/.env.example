# Configurações da API
# URL base da API backend
REACT_APP_API_URL=http://localhost:3002/api

# Porta do servidor de desenvolvimento (opcional)
# Se não especificado, usa a porta 3000 por padrão
PORT=3000
